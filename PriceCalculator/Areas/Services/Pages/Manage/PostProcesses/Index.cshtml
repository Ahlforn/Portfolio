@page
@model PriceCalculator.Areas.Services.Pages.Manage.PostProcesses.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Services/Pages/Manage/Shared/_DashboardLayout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PostProcesses[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostProcesses[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostProcesses[0].ExcelDefinedName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostProcesses[0].ExcelDefinedPriceName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PostProcesses) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExcelDefinedName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExcelDefinedPriceName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
